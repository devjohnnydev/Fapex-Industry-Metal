[phases.setup]
nixPkgs = ["nodejs_22", "curl"]
cmds = [
  "curl -fsSL https://github.com/pnpm/pnpm/releases/download/v10.26.1/pnpm-linux-x64 -o /usr/local/bin/pnpm && chmod +x /usr/local/bin/pnpm"
]

[phases.install]
cmds = ["pnpm install --ignore-scripts"]

[phases.build]
cmds = [
  "BASE_PATH=/ PORT=3000 NODE_ENV=production pnpm --filter @workspace/fapex-website run build",
  "NODE_ENV=production pnpm --filter @workspace/api-server run build"
]

[start]
cmd = "node --enable-source-maps artifacts/api-server/dist/index.mjs"
